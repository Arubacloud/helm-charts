apiVersion: v1
kind: ServiceAccount
metadata:
  name: certmanager-hook-sa
  namespace: cert-manager
  # No Helm hook annotations; standard chart resource
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: certmanager-hook-role
  namespace: cert-manager
  # No Helm hook annotations; standard chart resource
rules:
  - apiGroups: ['']
    resources: ['services']
    verbs: ['get']
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: certmanager-hook-rolebinding
  namespace: cert-manager
  # No Helm hook annotations; standard chart resource
subjects:
  - kind: ServiceAccount
    name: certmanager-hook-sa
    namespace: cert-manager
roleRef:
  kind: Role
  name: certmanager-hook-role
  apiGroup: rbac.authorization.k8s.io
